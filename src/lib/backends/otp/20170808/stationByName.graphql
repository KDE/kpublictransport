query stationsByName($name: String!) {
  stations: stops(name: $name) {
    gtfsId
    name
    lat
    lon
    parentStation {
      gtfsId
      name
      lat
      lon
      timezone
    }
    timezone
  }
}
