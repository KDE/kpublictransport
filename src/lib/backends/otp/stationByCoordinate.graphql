# SPDX-FileCopyrightText: 2020 Volker Krause <vkrause@kde.org>
# SPDX-License-Identifier: CC0-1.0

query stopsByCoordinate(
    $lat: Float!,
    $lon: Float!,
    $radius: Int!,
    $maxResults: Int!,
    $placeType: [FilterPlaceType]!
) {
    stopsByRadius: nearest(
        lat: $lat,
        lon: $lon,
        maxDistance: $radius,
        maxResults: $maxResults,
        filterByPlaceTypes: $placeType
    ) {
        edges {
            node {
                stop: place { ...on Stop {
                    id: gtfsId
                    name
                    lat
                    lon
                    timezone
                    parentStation {
                        id: gtfsId
                        name
                        lat
                        lon
                        timezone
                    }
                }}
                rentalVehicleStation: bikeRentalStation: place { ...bikeRentalStationFragment }
            }
        }
    }
}

fragment bikeRentalStationFragment on BikeRentalStation {
    id
    stationId
    name
    bikesAvailable
    spacesAvailable
    capacity
    state
    realtime
    allowDropoff
    allowOverloading
    isFloatingBike
    isCarStation
    networks
    lat
    lon
}
