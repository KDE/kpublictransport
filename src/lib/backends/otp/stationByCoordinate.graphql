query stopsByCoordinate($lat: Float!, $lon: Float!) {
  stopsByRadius(lat: $lat, lon: $lon, radius: 1000) {
    edges {
      node {
        stop {
          id: gtfsId
          name
          lat
          lon
          timezone
          parentStation {
            id: gtfsId
            name
            lat
            lon
            timezone
          }
        }
      }
    }
  }
}
