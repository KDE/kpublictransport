query stationsByName($name: String!) {
  stations: quays(name: $name) {
    id
    name
    lat: latitude
    lon: longitude
    timezone
    parentStation: stopPlace {
      id
      lat: latitude
      lon: longitude
      timezone
    }
    timezone
  }
}
