query stationsByName($name: String!) {
  stations(name: $name) {
    gtfsId
    name
    lat
    lon
    parentStation { gtfsId }
    #wheelchairBoarding
    timezone
    #alerts { alertHeaderText alertDescriptionText }
  }
}
